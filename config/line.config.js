var baseconfig = require('./config')
const config = {
    /** LINE **/
    channelAccessToken: baseconfig.line_channel_token,
    channelSecret: baseconfig.line_channel_secret
}


module.exports = config;